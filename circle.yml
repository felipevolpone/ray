
machine:
    python:
        version: 2.7.6

dependencies:
    pre:
        - sudo pip install coveralls
        - sudo pip install -r onhands-core/requirements.txt

test:
    override:
        - py.test onhands-core/tests/ --cov onhands-core/onhands/
    post:
        - coveralls


machine:
    python:
        version: 2.7.6

dependencies:
    pre:
        - sudo pip install coveralls
        - sudo pip install -r onhands-core/requirements.txt

test:
    override:
        - cd onhands-core;
        - ls -la
        - py.test tests/
    post:
        - coveralls

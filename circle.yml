
machine:
    python:
        version: 2.7.6

dependencies:
    pre:
        - sudo pip install -r onhands-core/requirements.txt

test:
    override:
        - cd onhans-core;
        - py.test tests/

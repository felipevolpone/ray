language: python

python:
  - 2.7

before_install:
    - pip install python-coveralls
    - pip install nose

script:
    - pip install -r ray-core/requirements.txt
    - cd ray-core/
    #- py.test tests/ --cov ray/
    - nosetests --tests=ray-core/tests --with-coverage
    # - pip install --editable .
    # - cd ..

    # - cd ray-sqlalchemy/
    # - pip install --editable .
    # - pip install requests
    # - cd tests/
    # - ray up --wsgifile app.py &
    # - sleep 2
    # - py.test test_integrated.py

    # - cd ../../ray-core
    # - py.test -v tests/ --cov ray


after_success:
  - coveralls --config_file=ray-core/.coveragerc

notifications:
    email:
        recipients:
            - felipevolpone@gmail.com
        on_success: change
        on_failure: always
